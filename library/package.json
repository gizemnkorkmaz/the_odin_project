{
  "name": "library",
  "version": "1.0.0",
  "description": "If you haven’t already, set up your project with skeleton HTML/CSS and JS files. All of your book objects are going to be stored in a simple array, so add a function to the script (not the constructor) that can take user’s input and store the new book objects into an array. Your code should look something like this:",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --watch assets/scss/main.scss:assets/scss/main.css --style compressed --no-source-map"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^6.0.1"
  }
}
